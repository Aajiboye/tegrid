# Example environment variables
PORT=8080
MONGODB_CONNECTION_STRING=mongodb://localhost:27017/trade_expert_grid
# Symmetric key used to encrypt NIN (must be 32 bytes). Provide as base64 or hex (64 hex chars)
# Example (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
# NIN_ENCRYPTION_KEY=base64-or-hex-value-here

